<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>



    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    
​
    <header class="text-center"></header>


    <header class="app-header__header">
    <h1>
      <a href="/">Media Ranker</a>
    
      <small>Ranking the Best of Everything</small>
    </h1>

    <nav class="app-header__nav">
      <ul class="nav app-header__site-nav-container">
        <li class="nav-item app-header__nav_item">
          <%= link_to "View Top Media", root_path, class:"nav-link" %>
        </li>
        <li class="nav-item app-header__nav_item">
          <a class="nav-link" href="/works">View all media</a>
        </li>
        <li class="nav-item app-header__nav_item">
          <a class="nav-link" href="/works/new">Add a new work</a>
        </li>
        <li class="nav-item app-header__nav_item">
          <a class="nav-link" href="/users">View all users</a>
        </li>
      </ul>
     
        

      <%if session[:user_id]%>
      <ul class="nav app-header__user-nav-container">
        <li class= "nav-item app-header__nav_item"><%=link_to "Logged in as #{session[:username]}", current_user_path(session[:user_id]), :class => "btn btn-primary"%></li>
        <li class= "nav-item app-header__nav_item"><%=link_to "Log out", logout_path, :class => "btn btn-primary", :method => :post %></li>
      </ul>
      <%elsif session[:id] == nil%>
      <ul class="nav app-header__user-nav-container">
        <li class= "nav-item app-header__nav_item"><%=link_to "Log In", login_path, :class => "btn btn-primary"%></li> 
      </ul>
      <% end %>

    </nav>

    <% flash.each do |name, message| %>
      <div class="alert alert-<%= name %>" role="alert">
        <%= message.html_safe %>
      </div>
    <% end %>
  </header>
​
    <main role="main">
      <%# render "shared/nav" %>
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
  </body>
</html>

  